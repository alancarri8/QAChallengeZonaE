{
"scripts": {
  "cy:run:chrome": "cypress run -b chrome",
  "cy:open:report": "start cypress/reports/html/index.html",
  "cy:run:test": "npm run cy:run:chrome && npm run cy:open:report"
},
  "devDependencies": {
    "@faker-js/faker": "^10.2.0",
    "cypress": "^15.10.0",
    "cypress-mochawesome-reporter": "^4.0.2"
  },
  "dependencies": {
    "dotenv": "^17.2.4"
  }
}
